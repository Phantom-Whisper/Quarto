<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"             
             xmlns:component="clr-namespace:QuartoApp.Components"
             x:Class="QuartoApp.Pages.ScorePage">

    <Grid Margin="20,20,20,50" 
       RowDefinitions="*,6*"
       ColumnDefinitions="*,3*">
        <Label Grid.Column="1"
            Text="Score"
            HorizontalOptions="Center"
            FontSize="Title"
            TextColor="{StaticResource Or }"/>
        <Grid ColumnDefinitions="*,3*"
           RowDefinitions="2*,*"
           Grid.Row="1"
           Grid.ColumnSpan="2">
            <VerticalStackLayout Margin="0,0,30,0">
                <component:CustomButton >
                </component:CustomButton>

                <component:CustomButton >
                </component:CustomButton>

                <component:CustomButton >
                </component:CustomButton>
            </VerticalStackLayout>

            <VerticalStackLayout Grid.Row="1" Margin="0,0,30,0">
                <component:CustomButton >
                </component:CustomButton>

                <component:CustomButton>
                </component:CustomButton>
            </VerticalStackLayout>
            <Image Source="back.png" 
                Grid.Column="1"
                Grid.RowSpan="2" ZIndex="0"
                Aspect="Fill"/>
            <ScrollView Grid.Column="1"
                     Grid.RowSpan="2">


            </ScrollView>
        </Grid>

    </Grid>

</ContentView>
